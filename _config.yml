# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: deltanicola
email: delta.meloni@gmail.com
author:
  avatar: "/assets/images/avatar.jpeg"
  name: "nicola meloni"
  bio: devops engineer / kubernetes, automation & infrastructure / focus on security, reliability and systems engineering
  links:
    - label: "email"
      icon: "fas fa-fw fa-envelope-square"
      url: "mailto:delta.meloni@gmail.com"
    - label: "github"
      icon: "fab fa-fw fa-github"
      url: "https://github.com/DeltaNicola"
    - label: "linkedin"
      icon: "fa-brands fa-linkedin"
      url: "https://www.linkedin.com/in/delta-nicolameloni/"
description: >- # this means to ignore newlines until "baseurl:"
  il blog di un devops engineer che smanetta con automazione, ai e tutto quello che si può scriptare
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://deltanicola.dev" # the base hostname & protocol for your site, e.g. http://example.com
github_username: deltanicola
atom_feed:
  hide: true
# Build settings
theme: minimal-mistakes-jekyll
minimal_mistakes_skin: "dark"
plugins:
  - jekyll-feed
  - jekyll-remote-theme
include: 
  - "_pages"

collections:
  projects:
    output: true
    permalink: /:collection/:path/

defaults:
  # _pages
  - scope:
      path: ""
      type: pages
    values:
      layout: single
      author_profile: true
      read_time: true
  # _posts
  - scope:
      path: ""
      type: posts
    values:
      layout: single
      author_profile: true
      read_time: true
      comments: # true
      share: true
      related: false
  # _portfolio
  - scope:
      path: ""
      type: projects
    values:
      layout: single
      author_profile: true
      share: true
      read_time: true

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
